# Contributing

Do not commit secrets, .env files, Supabase service role keys, marketplace app secrets, Google Drive tokens, database URLs with passwords, access tokens, refresh tokens, or local ZIP/debug archives.

Use .env.example or documentation placeholders instead.

Run flutter pub get and flutter analyze before opening a pull request.
